# IDX Flow Scanner v0.4.9

This release refreshes the Streamlit terminal UX and makes canonical result persistence automatic when the dedicated IDX Flow Supabase credentials are valid.

The persistence policy remains fail-closed: only the exact canonical project reference `djqvhbeonmicztxfisav` plus a non-empty service secret arms writes. Managed auto-run is not enabled automatically.

No production scoring formula, ranking threshold, Top-900 contract, execution authorization rule, predictive overlay policy, or Gate-15 promotion rule is changed by this release.
